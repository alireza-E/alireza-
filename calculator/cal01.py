
class cal() :
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def mul(self):
        a =(self.x*self.y)
        return a
    def div(self):
        a =(self.x/self.y)
        return a
    def plus(self):
        a =(self.x+self.y)
        return a
    def neg(self):
        a = (self.x - self.y)
        return a